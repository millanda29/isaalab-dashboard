services:
  dashboard:
    build: .
    container_name: isaalab-dashboard
    ports:
      - "8080:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /usr/bin/nsenter:/usr/bin/nsenter:ro
    pid: host
    restart: unless-stopped
    privileged: true
